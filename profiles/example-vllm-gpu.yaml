# =============================================================================
# ModelTainer Profile — M tier: OpenAI OSS 20B on GPU (vLLM)
# =============================================================================
# Model tier : M (Medium)
# Model      : openai/gpt-oss-20b
# Hardware   : GPU (NVIDIA)
# Port       : 8020
#
# Launch:  bash scripts/run_profile.sh profiles/example-vllm-gpu.yaml
# Dry-run: bash scripts/run_profile.sh --dry-run profiles/example-vllm-gpu.yaml
# =============================================================================

engine: vllm
hardware: gpu
model: openai/gpt-oss-20b
port: 8020                # M tier port

model_params:
  max_model_len: 4096
  gpu_memory_utilization: 0.90
  dtype: auto

engine_args: []
